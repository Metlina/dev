<Page
    x:Class="Leviton.SnapLink.Views.RoomsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Leviton.SnapLink.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vh="using:Leviton.Common.ViewHelper"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="RoomsDataTemplate">
            <Grid vh:Alt.ColumnDefinitions="Auto, *">

                <TextBlock Grid.Column="1"
                           Text="{Binding Name}"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <ListView ItemTemplate="{StaticResource RoomsDataTemplate}"
                  ItemsSource="{Binding Rooms}"
                  />
        
    </Grid>
</Page>
